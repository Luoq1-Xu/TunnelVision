{
  "buildCommand": "cd packages/web && npm install && npm run build",
  "outputDirectory": "packages/web/.next",
  "framework": "nextjs",
  "installCommand": "npm install && cd packages/web && npm install"
}
